const num1 = 100
const num2 = 50

let val;

// arithmetic
val = num1 + num2
val = num1 - num2
val = num1 * num2
val = num1 / num2
val = num1 % num2

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.8);
val = Math.ceil(2.4); // returns next highest integer
val = Math.floor(2.8); // return next lowest integer
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(8,2) // first pass number, then power to raise
val = Math.min(2,33,44,56)
val = Math.max(2,33,44,56)
val = Math.random();
val = Math.floor(Math.random() * 20 +1) // multiply a random number by the max number I would want

console.log(val)
